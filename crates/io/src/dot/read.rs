use std::collections::HashMap;

use graph::Models;
use log::{debug, error};

pub fn read(input_file: &str) -> Models {
    debug!("{}", input_file);
    error!("Feature not implemented");
    let models = Models { models: HashMap::new() };
    return models;
}
