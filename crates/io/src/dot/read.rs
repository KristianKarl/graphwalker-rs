use graph::Models;
use log::{debug, error};

pub fn read(input_file: &str) -> Models {
    debug!("{}", input_file);
    error!("Feature not implemented");
    let models = Models { models: vec![] };
    return models;
}
